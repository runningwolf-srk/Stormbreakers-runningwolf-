import {ALL_RELICS} from "@/data/armory"; import {CANON} from "@/data/constants"
export default function Chronicles(){ return <div className="max-w-6xl mx-auto px-6 py-10"><h1 className="text-4xl font-black">📖 CHRONICLES — Walk the Testimony</h1><p className="text-[11px] font-mono text-white/30 mt-2">{CANON.TAGLINE}</p><p className="mt-4 text-white/50 text-sm max-w-2xl">{CANON.MISSION} Every relic has a battle behind it.</p><div className="mt-10 grid md:grid-cols-2 gap-3">{ALL_RELICS.map(r=><a key={r.slug} href={`/chronicles/${r.slug}`} className="flex justify-between border border-white/10 rounded-xl p-4 bg-zinc-900/20 hover:bg-zinc-900/40"><span className="text-xs font-bold">{r.symbol} RELIC {r.num} • {r.title}</span><span className="text-[10px] font-mono text-white/30">{r.status}</span></a>)}</div></div> }
