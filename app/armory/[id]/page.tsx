import Link from 'next/link'
import { CANON } from '@/lib/constants'
import { RelicIcon } from '@/components/relics/RelicIcon'
export default function RelicPage({ params }: { params: { id: string } }) {
  const r = CANON.find(x => x.id === Number(params.id)) || CANON[0]
  return (<main className="px-4 py-12"><div className="max-w-3xl mx-auto"><Link href="/armory" className="text-amber-400/40 text-xs mb-8 inline-block">← BACK TO ARMORY</Link><div className="text-center mb-8"><div className="w-20 h-20 mx-auto rounded-full bg-amber-500/10 border border-amber-400/20 flex items-center justify-center text-amber-400 mb-4"><RelicIcon symbol={r.symbol} className="w-10 h-10" /></div><p className="text-amber-400/40 text-xs font-black">{r.act}</p><h1 className="text-4xl font-black text-amber-100 mt-2">{r.title} — {r.song}</h1></div><div className="p-5 bg-stone-900/60 border border-amber-400/10 rounded-xl mb-6"><p className="text-amber-400/40 text-xs font-black">SONG {String(r.id).padStart(2,'0')}/16</p><audio controls className="w-full mt-3"><source src={`/audio/${String(r.id).padStart(2,'0')}-preview.mp3`} /></audio></div><div className="p-6 bg-amber-500/5 border border-amber-400/20 rounded-xl mb-6"><p className="text-amber-400/50 text-xs font-black">SCRIPTURE — {r.ref}</p><p className="text-amber-100 italic mt-3">"{r.verse}"</p>{r.verse2 && <p className="text-amber-200/60 italic mt-4 text-sm">"{r.verse2}"</p>}{r.verse3 && <p className="text-amber-100 italic mt-4">"{r.verse3}"</p>}{r.verse4 && <p className="text-amber-300 font-bold mt-4">"{r.verse4}"</p>}</div><div className="p-6 bg-stone-900/40 border border-amber-400/10 rounded-xl mb-6"><p className="text-amber-400/50 text-xs font-black">TESTIMONY</p><p className="text-amber-100/80 italic mt-3">"{r.reflection}"</p><p className="text-stone-300 text-sm mt-4 whitespace-pre-line">{r.story}</p></div><div className="p-6 bg-stone-900 border border-amber-400/20 rounded-xl mb-6"><p className="text-amber-400 text-xs font-black">LYRICS — {r.song}</p><p className="text-amber-100 font-black text-sm mt-4 whitespace-pre-line">{r.lyrics}</p></div><div className="grid grid-cols-2 gap-4"><div className="p-5 bg-stone-900 border border-amber-400/20 rounded-xl"><p className="text-amber-400 text-xs font-black">DECLARATION</p><p className="text-amber-100 font-bold mt-3">"{r.declaration}"</p></div><div className="p-5 bg-amber-500/5 border border-amber-400/20 rounded-xl"><p className="text-amber-400 text-xs font-black">PRAYER</p><p className="text-amber-100/80 italic text-sm mt-3">"{r.prayer}"</p></div></div></div></main>)
}
