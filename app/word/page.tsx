import {ALL_RELICS} from "@/data/armory"; import {CANON} from "@/data/constants"; import Link from "next/link"
export default function WordIndex(){ return <div className="max-w-6xl mx-auto px-6 py-10"><h1 className="text-4xl font-black tracking-tighter">✝️ WORD — Stand on Scripture</h1><p className="text-[11px] font-mono text-white/30 mt-2">{CANON.TAGLINE}</p><p className="text-[11px] font-mono text-white/30">{CANON.STATUS_LINE}</p><div className="mt-8 grid md:grid-cols-2 gap-4">{ALL_RELICS.map(r=><Link key={r.slug} href={`/word/${r.slug}`} className="border border-white/10 rounded-2xl p-5 bg-zinc-900/30 hover:border-[#D4AF37]/30"><div className="text-[10px] font-mono text-white/30">RELIC {r.num} • {r.status}</div><div className="font-bold text-sm mt-1">{r.symbol} {r.title} — {r.verse}</div></Link>)}</div></div> }
